greet resource test quarkus test public class greet resource test after each public void clean up cluster builder slot reset cluster node test public void test sentinel jax rs quarkus adapter give when get hello txt then status code body be hello give when get hello txt then status code javax w rs core response status too many request get status code body be block by sentinel flow limit test public void test sentinel annotation quarkus adapter give when get hello fallback a then status code body be hello a give when get hello fallback b then status code body be hello b give when get hello fallback degrade then status code body be global default fallback ex test sentinel fallback give when get hello fallback degrade then status code body be global block handler ex null give when get hello fallback a then status code body be global block handler ex null give when get hello fallback2 a then status code body be hello a give when get hello fallback2 b then status code body be hello b give when get hello fallback2 degrade then status code body be greet fallback degrade give when get hello fallback2 degrade then status code body be greet fallback degrade give when get hello fallback2 a then status code body be greet fallback a 
